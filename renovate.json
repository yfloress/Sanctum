{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>yfloress/renovate-config"],
  "description": [
    "The policy lives in the shared preset. Only what is true of this",
    "repository alone belongs here.",
    "The Astro site lives on its own branch with its own flake and lockfile.",
    "Renovate reads this config from the default branch and applies it to",
    "both, which is what Dependabot could not do: an entry with a",
    "target-branch never received security updates."
  ],
  "baseBranchPatterns": ["main", "web"]
}
